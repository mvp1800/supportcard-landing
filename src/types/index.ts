export interface WaitlistEntry {
  name: string
  email: string
}

export interface Feature {
  icon: string
  title: string
  description: string
}

export interface ToastState {
  name: string
  email: string
}
